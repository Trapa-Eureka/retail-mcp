/**
 * 재주문 제안 에이전트 진입점 (자리표시자).
 * 실제 오케스트레이션은 T8에서 구현한다 (DESIGN.md §7, docs/TASKS.md).
 * `npm run agent:reorder`는 이 파일이 완성되기 전까지 명확한 안내 메시지와 함께 종료 코드 1을 반환한다.
 */
console.error(
  "재주문 에이전트는 아직 구현되지 않았습니다 (T8 예정). " +
    "docs/TASKS.md의 T8 — 재주문 에이전트 태스크가 완료되면 `npm run agent:reorder`로 실행됩니다.",
);
process.exit(1);
