node_modules/
dist/
coverage/
.env*
!.env.example
*.log
*.tgz
.DS_Store
# Output of a local npm sbom run (QA-006, TASKS T35) — CI uploads it as an artifact, never commits it.
sbom.json
# Embedded PGlite data (default path when DATABASE_URL is unset, T14) — local data, never commit.
.retail-mcp/
# Claude Code local session artifacts (e.g. schedule lock files) — not repository content; found and added in TASKS T36.
.claude/
# Settings-screen captures (npm token issuance, GitHub secret/environment etc.) — files a human keeps in docs/ for reference;
# they show account screens, so they are not committed to the public repository (T37, 2026-09-04).
docs/*.png
