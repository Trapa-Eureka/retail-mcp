/**
 * 수동 스모크 스크립트 진입점 (자리표시자) — 사람 전용, 실 Loyverse + 실 DB 대상.
 * 실제 구현은 T11에서 한다 (docs/TESTING.md §5, docs/TASKS.md).
 * `npm run smoke`는 이 파일이 완성되기 전까지 명확한 안내 메시지와 함께 종료 코드 1을 반환한다.
 */
console.error(
  "스모크 스크립트는 아직 구현되지 않았습니다 (T11 예정). " +
    "docs/TASKS.md의 T11 — 스모크 + 문서 갱신 태스크가 완료되면 `npm run smoke`로 실행됩니다.",
);
process.exit(1);
